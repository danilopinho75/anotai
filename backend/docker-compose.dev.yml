version: "3"

services:
  app:
    build: .
    image: anotai-nodejs
    container_name: anotai-backend
    command: npm run dev
    ports: 
      - 3000:3000
    volumes:
      - .:/usr/app
      - /usr/app/node_modules